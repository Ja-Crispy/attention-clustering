torch>=2.1.0
numpy>=1.24.0
muon-optimizer
scikit-learn
matplotlib
seaborn
tqdm
wandb
transformer-lens
# tensorlens: install separately via `git clone https://github.com/idoatad/TensorLens.git && cd TensorLens && uv sync`
